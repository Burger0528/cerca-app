// SALVADOR: sustituir el import por tu `SignInScreen` (React Hook Form + zodResolver).
// El archivo de ruta se queda en una línea a propósito: la pantalla es de `presentation`,
// esto solo dice qué se pinta en `/sign-in`.
export { SignInPlaceholder as default } from '../../presentation/screens/sign-in-placeholder';
